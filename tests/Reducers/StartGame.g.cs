// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void StartGameHandler(ReducerEventContext ctx, ulong gameId);
        public event StartGameHandler? OnStartGame;

        public void StartGame(ulong gameId)
        {
            conn.InternalCallReducer(new Reducer.StartGame(gameId));
        }

        public bool InvokeStartGame(ReducerEventContext ctx, Reducer.StartGame args)
        {
            if (OnStartGame == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch(ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnStartGame(
                ctx,
                args.GameId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class StartGame : Reducer, IReducerArgs
        {
            [DataMember(Name = "game_id")]
            public ulong GameId;

            public StartGame(ulong GameId)
            {
                this.GameId = GameId;
            }

            public StartGame()
            {
            }

            string IReducerArgs.ReducerName => "start_game";
        }
    }
}
