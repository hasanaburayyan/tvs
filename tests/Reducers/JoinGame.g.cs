// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void JoinGameHandler(ReducerEventContext ctx, ulong gameId);
        public event JoinGameHandler? OnJoinGame;

        public void JoinGame(ulong gameId)
        {
            conn.InternalCallReducer(new Reducer.JoinGame(gameId));
        }

        public bool InvokeJoinGame(ReducerEventContext ctx, Reducer.JoinGame args)
        {
            if (OnJoinGame == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch(ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnJoinGame(
                ctx,
                args.GameId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class JoinGame : Reducer, IReducerArgs
        {
            [DataMember(Name = "game_id")]
            public ulong GameId;

            public JoinGame(ulong GameId)
            {
                this.GameId = GameId;
            }

            public JoinGame()
            {
            }

            string IReducerArgs.ReducerName => "join_game";
        }
    }
}
